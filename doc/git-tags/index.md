+++
+++

# git tags

```gitconfig
# git tags: list tags
#
# This alias is because friendly wording is easier to remember.
# We use these friendly wording plurals: branches, stashes, tags.
# Thanks to http://gggritso.com/human-git-aliases
#
tags = tag -n1 --list
```
